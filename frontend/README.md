# Inventory App

Backend: Express + Mongo (port 4000)  
Frontend: Next.js + Tailwind (port 3000)

## Run locally

Start MongoDB (e.g. `mongod`), then:

# backend
cd backend
npm install
npm run dev

# frontend
cd frontend
npm install
npm run dev
